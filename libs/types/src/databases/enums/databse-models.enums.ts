import { PropertyEnums } from "../../properties/enums/database-model.enum";

export enum DatabseEnums {
    DATABASE_CONNECTION = 'DATABASE_CONNECTION',
}

export const DatabseModelEnums = {
    ...DatabseEnums,
    ...PropertyEnums,

}